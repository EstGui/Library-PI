import styled from 'styled-components';

export const NewsRegistryContainer = styled.div `
  width: 100%;
  height: 440px;
  margin: 60px 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: space-evenly;
  box-shadow: 0 0 50px rgba(0, 0, 0, 0.5);
`